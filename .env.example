# Copy this file to `.env` and set your own values.
# `.env` is gitignored — never commit real secrets.

# JupyterLab login password (required to start the container)
JUPYTER_PASSWORD=change-me-to-a-strong-password
